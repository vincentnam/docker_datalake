# Web Service

Run

- production
    ```shell
    docker-compose up -d
    ```
- development
    ```shell
    docker-compose -f docker-compose-dev.yml up -d
    ```

Stop

```shell
docker-compose down
```

Monitor log

```shell
docker-compose logs -f
```
